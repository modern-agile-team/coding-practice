/**
 * 10.2 위상 정렬  [프로그래머스 Lv3 / 백준 Gold III]
 *
 * 정점 수 n(0..n-1)과 방향 간선 목록 edges([from, to]: from을 먼저 해야 to 가능)를
 * 받아 위상 정렬 순서를 배열로 반환하세요.
 * 선택 가능한 정점이 여러 개면 번호가 작은 정점을 먼저 선택합니다(사전순 최소).
 * 사이클이 있어 정렬이 불가능하면 빈 배열을 반환합니다.
 *
 * @example
 * solution(4, [[0, 1], [0, 2], [1, 3], [2, 3]]) // => [0, 1, 2, 3]
 */
export function solution(n: number, edges: [from: number, to: number][]): number[] {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
