/**
 * 10.4 최소 신장 트리 (크루스칼)  [프로그래머스 Lv3 / 백준 Gold IV]
 *
 * 정점 수 n(0..n-1)과 무방향 가중 간선 목록 edges를 받아,
 * 모든 정점을 연결하는 최소 신장 트리(MST)의 간선 가중치 합을 반환하세요.
 * 그래프가 연결되어 있지 않으면 -1을 반환합니다.
 *
 * @example
 * solution(4, [[0, 1, 1], [1, 2, 2], [0, 2, 2], [2, 3, 3]]) // => 6 (1+2+3)
 */
export type Edge = [a: number, b: number, weight: number];

export function solution(n: number, edges: Edge[]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
