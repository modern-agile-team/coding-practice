/**
 * 5.4 후위 표기식 계산  [프로그래머스 Lv2 / 백준 Silver III]
 *
 * 후위 표기식(postfix) 토큰 배열을 계산한 결과를 반환하세요.
 * 토큰은 숫자 문자열 또는 연산자 "+", "-", "*", "/"이고,
 * 나눗셈은 0 방향으로 버리는 정수 나눗셈입니다.
 *
 * @example
 * solution(["5", "1", "2", "+", "*"]) // => 15  (5 * (1+2))
 */
export function solution(tokens: string[]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
