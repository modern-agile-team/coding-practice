/**
 * 5.2 동전 최소 개수 (그리디)  [프로그래머스 Lv1 / 백준 Silver IV]
 *
 * 동전 금액 목록 coins로 amount를 만들 때 필요한 최소 동전 개수를 반환하세요.
 * coins는 내림차순이며 큰 단위가 작은 단위의 배수인 화폐 체계입니다.
 * amount를 정확히 만들 수 없으면 -1을 반환합니다.
 *
 * @example
 * solution([500, 100, 50, 10], 1260) // => 6  (500×2 + 100×2 + 50 + 10)
 */
export function solution(coins: number[], amount: number): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
