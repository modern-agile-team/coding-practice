/**
 * 5.3 올바른 괄호  [프로그래머스 Lv2 / 백준 Silver III]
 *
 * '(' 와 ')' 로만 이루어진 문자열 s가 올바른 괄호 문자열인지 판별하세요.
 * 모든 여는 괄호가 짝이 맞게 닫히고, 닫는 괄호가 먼저 나오면 안 됩니다.
 *
 * @example
 * solution("(())") // => true
 * solution(")(")   // => false
 */
export function solution(s: string): boolean {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
