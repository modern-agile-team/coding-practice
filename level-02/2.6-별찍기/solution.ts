/**
 * 2.6 별 찍기 (오른쪽 정렬)  [프로그래머스 Lv0 / 백준 Bronze II]
 *
 * 정수 n을 받아 오른쪽 정렬 삼각형을 문자열 배열로 반환하세요.
 * i번째 줄(1부터)은 공백 (n-i)개 뒤에 별('*') i개가 옵니다.
 *
 * @example
 * solution(3) // => ["  *", " **", "***"]
 */
export function solution(n: number): string[] {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
