/**
 * 2.3 배열 집계  [프로그래머스 Lv0 / 백준 Bronze III]
 *
 * 정수 배열을 받아 최솟값·최댓값·합계·평균을 객체로 반환하세요.
 * 평균(avg)은 실수이며, 빈 배열은 입력되지 않습니다.
 *
 * @example
 * solution([1, 2, 3, 4]) // => { min: 1, max: 4, sum: 10, avg: 2.5 }
 */
export interface ArrayStats {
  min: number;
  max: number;
  sum: number;
  avg: number;
}

export function solution(numbers: number[]): ArrayStats {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
