/**
 * 6.2 조합으로 만드는 합의 종류 (완전탐색)  [프로그래머스 Lv2 / 백준 Silver II]
 *
 * 서로 다른 정수 배열 numbers에서 서로 다른 k개를 골라 더했을 때,
 * 나올 수 있는 서로 다른 합의 개수를 반환하세요.
 * (0 <= k <= numbers.length, k가 0이면 합은 0 한 가지)
 *
 * @example
 * solution([1, 2, 3], 2) // => 3  (1+2=3, 1+3=4, 2+3=5)
 */
export function solution(numbers: number[], k: number): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
