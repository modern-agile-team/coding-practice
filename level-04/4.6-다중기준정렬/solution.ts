/**
 * 4.6 다중 기준 정렬  [프로그래머스 Lv2 / 백준 Silver IV]
 *
 * 사람 목록 people을 나이(age) 오름차순, 나이가 같으면 이름(name) 사전순으로
 * 정렬한 새 배열을 반환하세요. 원본은 변경하지 마세요.
 *
 * @example
 * solution([
 *   { name: "Bob", age: 30 },
 *   { name: "Amy", age: 30 },
 *   { name: "Cara", age: 20 },
 * ])
 * // => [{ Cara,20 }, { Amy,30 }, { Bob,30 }]
 */
export interface Person {
  name: string;
  age: number;
}

export function solution(people: Person[]): Person[] {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
