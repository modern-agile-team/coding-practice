/**
 * 4.7 전화번호 접두어 검사  [프로그래머스 Lv2 / 백준 Silver III]
 *
 * 전화번호 문자열 목록 phones에서 어떤 번호가 다른 번호의 접두어인 경우가
 * 하나라도 있으면 false, 전혀 없으면 true를 반환하세요.
 *
 * @example
 * solution(["119", "97674223", "1195524421"]) // => false
 * solution(["123", "456", "789"])             // => true
 */
export function solution(phones: string[]): boolean {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
