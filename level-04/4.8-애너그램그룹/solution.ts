/**
 * 4.8 애너그램 그룹  [프로그래머스 Lv2 / 백준 Silver IV]
 *
 * 소문자 단어 목록 words에서 서로 애너그램(글자 구성이 같은 단어)인
 * 단어끼리 묶어 반환하세요.
 * 그룹 내부와 그룹들의 순서는 모두 처음 등장한 순서를 따릅니다.
 *
 * @example
 * solution(["eat", "tea", "tan", "ate", "nat", "bat"])
 * // => [["eat", "tea", "ate"], ["tan", "nat"], ["bat"]]
 */
export function solution(words: string[]): string[][] {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
