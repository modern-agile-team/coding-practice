/**
 * 4.1 최빈값  [프로그래머스 Lv1 / 백준 Silver IV]
 *
 * 정수 배열에서 가장 많이 등장한 값을 반환하세요.
 * 최빈값이 여러 개면 그중 가장 작은 값을 반환합니다. 빈 배열은 입력되지 않습니다.
 *
 * @example
 * solution([1, 2, 2, 3, 3]) // => 2  (2와 3이 동률 → 더 작은 2)
 */
export function solution(numbers: number[]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
