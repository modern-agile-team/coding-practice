/**
 * 3.1 회문 판별  [프로그래머스 Lv1 / 백준 Silver V]
 *
 * 문자열 s가 회문(앞뒤로 읽어도 같은 문자열)인지 판별해 boolean으로 반환하세요.
 * 대소문자는 구분하지 않고 공백은 무시하며, 알파벳/숫자/공백만 들어옵니다.
 *
 * @example
 * solution("level") // => true
 * solution("hello") // => false
 */
export function solution(s: string): boolean {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
