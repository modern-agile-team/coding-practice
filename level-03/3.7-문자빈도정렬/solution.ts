/**
 * 3.7 문자 빈도 정렬  [프로그래머스 Lv1 / 백준 Silver V]
 *
 * 소문자 문자열 s의 고유 문자들을 등장 빈도가 높은 순으로 정렬한 배열을 반환하세요.
 * 빈도가 같으면 사전순(작은 문자 먼저)입니다.
 *
 * @example
 * solution("banana") // => ["a", "n", "b"]  (a:3, n:2, b:1)
 */
export function solution(s: string): string[] {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
