/**
 * 1.7 부호 분류  [프로그래머스 Lv0 / 백준 Bronze IV]
 *
 * 정수 배열을 받아 양수·음수·0의 개수를 객체로 반환하세요.
 *
 * @example
 * solution([1, -2, 0, 3, 0, -5])
 * // => { positive: 2, negative: 2, zero: 2 }
 */
export interface SignCount {
  positive: number;
  negative: number;
  zero: number;
}

export function solution(numbers: number[]): SignCount {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
