/**
 * 9.2 가장 긴 증가 부분 수열 (LIS)  [프로그래머스 Lv3 / 백준 Gold V]
 *
 * 정수 배열 numbers에서 강한 증가(뒤 원소가 앞 원소보다 큼) 부분 수열 중
 * 가장 긴 것의 길이를 반환하세요.
 * 부분 수열은 원래 순서를 유지하되 연속일 필요는 없습니다.
 * numbers의 길이는 최대 5,000입니다.
 *
 * @example
 * solution([10, 20, 10, 30, 20, 50]) // => 4  (10, 20, 30, 50)
 */
export function solution(numbers: number[]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
