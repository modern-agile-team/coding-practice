/**
 * 9.5 최장 공통 부분 수열 (LCS)  [프로그래머스 Lv3 / 백준 Gold V]
 *
 * 두 문자열 a, b의 가장 긴 공통 부분 수열(순서 유지, 연속일 필요 없음)의
 * 길이를 반환하세요.
 *
 * @example
 * solution("ACAYKP", "CAPCAK") // => 4  (ACAK)
 */
export function solution(a: string, b: string): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
