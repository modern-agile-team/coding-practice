/**
 * 7.1 이분 탐색  [프로그래머스 Lv2 / 백준 Silver IV]
 *
 * 오름차순 정렬된 배열 sorted에서 target의 인덱스를 찾아 반환하세요. 없으면 -1.
 * 값은 중복되지 않으며, sorted의 길이는 최대 1,000,000입니다.
 *
 * @example
 * solution([1, 3, 5, 7, 9], 7) // => 3
 * solution([1, 3, 5, 7, 9], 4) // => -1
 */
export function solution(sorted: number[], target: number): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
