/**
 * 7.5 구간 합 질의 (누적합)  [프로그래머스 Lv2 / 백준 Silver III]
 *
 * 각 질의 [l, r](0-기반, 양 끝 포함)에 대해 numbers[l..r]의 합을 구해
 * 질의 순서대로 배열로 반환하세요.
 * numbers의 길이는 최대 200,000, 질의 수는 최대 1,000,000입니다.
 *
 * @example
 * solution([1, 2, 3, 4, 5], [[0, 2], [1, 3], [0, 4]]) // => [6, 9, 15]
 */
export function solution(
  numbers: number[],
  queries: [l: number, r: number][],
): number[] {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
