/**
 * 7.3 두 수의 합 (투 포인터)  [프로그래머스 Lv2 / 백준 Silver III]
 *
 * 오름차순 정렬된 배열 sorted에서 두 원소의 합이 target이 되는
 * 인덱스 쌍 [i, j] (i < j)를 반환하세요. 없으면 null.
 * 정답이 여러 쌍이면 i가 가장 작은 쌍을, i가 같으면 j가 가장 큰 쌍을 반환합니다.
 *
 * @example
 * solution([1, 2, 4, 7, 11], 9) // => [1, 3]  (2 + 7)
 */
export function solution(
  sorted: number[],
  target: number,
): [i: number, j: number] | null {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
