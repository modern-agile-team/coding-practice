/**
 * 8.4 미로 탈출 (BFS)  [프로그래머스 Lv3 / 백준 Silver I]
 *
 * 'S'(시작), 'E'(출구), '.'(길), '#'(벽)으로 이루어진 미로에서 상하좌우로 이동할 때,
 * S에서 E까지 "지나는 칸 수"(S와 E 포함)의 최솟값을 반환하세요.
 * (8.1과 달리 이동 횟수가 아니라 칸 수입니다)
 * 도달할 수 없으면 -1이며, S와 E는 각각 정확히 하나씩 존재합니다.
 *
 * @example
 * solution(["S..", "##.", "..E"]) // => 5
 */
export function solution(maze: string[]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
