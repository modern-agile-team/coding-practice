/**
 * 8.3 섬의 개수  [프로그래머스 Lv3 / 백준 Silver I]
 *
 * 0(바다)과 1(땅)로 이루어진 격자에서 상하좌우로 연결된
 * 땅 덩어리(섬)의 개수를 반환하세요. 대각선은 연결로 보지 않습니다.
 *
 * @example
 * solution([
 *   [1, 1, 0, 0],
 *   [1, 0, 0, 1],
 *   [0, 0, 1, 1],
 * ]) // => 2
 */
export function solution(grid: number[][]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
