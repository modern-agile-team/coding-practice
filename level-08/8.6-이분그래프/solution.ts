/**
 * 8.6 이분 그래프 판별  [프로그래머스 Lv3 / 백준 Gold IV]
 *
 * 정점 수 n(0..n-1)과 무방향 간선 목록 edges를 받아, 그래프가
 * 이분 그래프(정점을 두 그룹으로 나눠 모든 간선이 서로 다른 그룹을 잇게
 * 할 수 있는 그래프)인지 반환하세요. 그래프는 연결되어 있지 않을 수 있습니다.
 *
 * @example
 * solution(3, [[0, 1], [1, 2], [2, 0]]) // => false (홀수 사이클)
 */
export function solution(n: number, edges: [a: number, b: number][]): boolean {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
