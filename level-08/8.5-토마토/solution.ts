/**
 * 8.5 토마토 (다중 시작 BFS)  [프로그래머스 Lv3 / 백준 Gold V]
 *
 * 격자 grid(1=익은 토마토, 0=안 익은 토마토, -1=빈 칸)에서 매일
 * 익은 토마토가 상하좌우 인접한 안 익은 토마토를 익힙니다.
 * 모든 토마토가 익는 최소 일수를 반환하세요.
 * 모두 익힐 수 없으면 -1, 처음부터 모두 익어 있으면 0입니다.
 *
 * @example
 * solution([[0, 0, 0], [0, 0, 0], [0, 0, 1]]) // => 4
 */
export function solution(grid: number[][]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
