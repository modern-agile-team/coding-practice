/**
 * 8.2 연결 요소의 개수  [프로그래머스 Lv3 / 백준 Silver II]
 *
 * 정점 수 n(0..n-1)과 무방향 간선 목록 edges를 받아,
 * 그래프의 연결 요소(서로 연결된 정점 그룹) 개수를 반환하세요.
 *
 * @example
 * solution(5, [[0, 1], [1, 2], [3, 4]]) // => 2
 */
export function solution(n: number, edges: [a: number, b: number][]): number {
  // TODO: 구현하세요
  throw new Error("아직 구현되지 않았습니다");
}
